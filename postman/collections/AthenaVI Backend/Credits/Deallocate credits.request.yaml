$kind: http-request
description: TEAM OWNER only. Moves workspace pool credits back to personal.
url: "{{baseUrl}}/api/credits/{{workspaceId}}/deallocate"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "amount": 1000
    }
order: 1300
