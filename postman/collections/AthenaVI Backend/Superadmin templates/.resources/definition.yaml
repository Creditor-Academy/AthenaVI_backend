$kind: collection
description: Platform superadmin only. Shared CRUD for DECK_LAYOUT (PPT), DECK_PACK (multi-slide), and VIDEO_SCENE (video). type is required on create. Bare-minimum packs need schemaVersion 2, meta/narrative, per-slide intent/designTokens/generationHints/placeholder (imagePrompt on visual slides), and generationDefaults.
order: 12000
