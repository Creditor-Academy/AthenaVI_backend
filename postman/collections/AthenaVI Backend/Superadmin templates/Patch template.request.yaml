$kind: http-request
url: "{{baseUrl}}/api/superadmin/templates/{{deckTemplateId}}"
method: PATCH
headers:
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Title Centered Custom (updated)",
      "isActive": true,
      "contentType": "title",
      "variant": "v2"
    }
order: 5000
