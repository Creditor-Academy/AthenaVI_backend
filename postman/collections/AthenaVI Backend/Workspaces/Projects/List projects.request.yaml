$kind: http-request
description: Summary cards only (no `data`). Includes owner, lastModifiedAt/By, storageBytes. Optional folderId query.
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/projects?folderId={{folderId}}"
method: GET
order: 3000
