$kind: http-request
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/members/{{memberId}}/role"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "role": "ADMIN"
    }
order: 11000
