$kind: http-request
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/folders"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "New Folder"
    }
order: 13000
