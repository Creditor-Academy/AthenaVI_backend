$kind: http-request
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/presentations/{{presentationId}}/slides/{{slideId}}/elements/{{elementId}}"
method: DELETE
headers:
  Authorization: Bearer {{accessToken}}
order: 5000
