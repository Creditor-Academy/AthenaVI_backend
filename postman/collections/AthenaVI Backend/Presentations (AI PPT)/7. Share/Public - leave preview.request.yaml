$kind: http-request
description: Best-effort leave on unload. Silent viewers are dropped after the presence TTL anyway.
url: "{{baseUrl}}/api/p/{{presentationShareToken}}/presence?viewerSessionId={{viewerSessionId}}"
method: DELETE
order: 9000
