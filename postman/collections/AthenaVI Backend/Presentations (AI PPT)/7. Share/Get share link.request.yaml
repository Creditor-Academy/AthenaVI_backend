$kind: http-request
description: Metadata only - masked urlDisplay, never the raw token.
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/presentations/{{presentationId}}/share"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
order: 2000
