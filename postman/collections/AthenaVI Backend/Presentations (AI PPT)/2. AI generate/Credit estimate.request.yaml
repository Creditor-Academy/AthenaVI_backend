$kind: http-request
url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/presentations/{{presentationId}}/credit-estimate?slideCount=10"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
order: 1000
