$kind: collection
description: |-
  AI Image Gen (OpenAI): models/formats/styles catalogs, generate (image/infographic/social), regenerate, tweak, download (png/jpg/pdf).

  Requires `OPENAI_API_KEY`. Charges workspace credits on success. Downloads are free.
auth:
  - id: image-gen-bearer-auth
    type: bearer
    name: bearer auth
    credentials:
      token: "{{accessToken}}"
order: 8500
